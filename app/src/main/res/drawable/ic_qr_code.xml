<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">

    <!-- Carré en haut à gauche -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M3,3h8v8H3z"/>

    <!-- Carré en haut à droite -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M13,3h8v8h-8z"/>

    <!-- Carré en bas à gauche -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M3,13h8v8H3z"/>

    <!-- Petit carré en bas à droite (coordonnées directes) -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M15,15h2v2h-2z"/>
</vector>